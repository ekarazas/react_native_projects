import { View, Text, ScrollView, SafeAreaView } from "react-native";
import { useState } from "react";

const Home = () => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
